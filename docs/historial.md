# Historial de desarrollo

Este archivo registra avances realizados. Las ideas futuras no implican que ya estén implementadas ni que haya que desarrollarlas sin una nueva petición.

## 2026-09-14 — Primera ejecución de un archivo

- **Qué se hizo:** lectura de una ruta recibida por argumento y ejecución de `print("texto")` mediante una comprobación directa del texto.
- **Por qué:** completar el primer paso mínimo: abrir `hello.oki` e imprimir `Hello world`.
- **Archivos:** `src/main.rs` y `hello.oki`.
- **Validación realizada:** compilación y ejecución del ejemplo con salida `Hello world`; comprobación de formato con Cargo.
- **Evolución:** esta implementación fue sustituida por las etapas descritas en la siguiente entrada.

## 2026-09-14 — Base de un intérprete de árbol

- **Qué se hizo:** scanner con tokens y líneas, parser descendente, nodos `Expr` y `Stmt`, e intérprete que ejecuta el AST. Se mantiene únicamente la instrucción `print` con cadenas y se permite una lista de instrucciones.
- **Por qué:** seguir el enfoque de *Crafting Interpreters* y disponer de etapas que se puedan ampliar progresivamente.
- **Archivos:** `src/main.rs` y `README.md`; se conserva `hello.oki` como ejemplo y entrada de una prueba.
- **Errores:** el análisis del archivo completo precede a la ejecución; los errores léxicos y sintácticos indican la línea. Los errores de escritura también se propagan.
- **Validación realizada:** seis pruebas superadas; ejecución de `hello.oki` con salida `Hello world`; comprobaciones de formato y Clippy con `-D warnings` superadas.

## 2026-09-14 — Documentación para aprender y continuar

- **Qué se hizo:** documentación del estado actual, explicación del recorrido de `print` con tokens y AST, y este historial de avances. Se añadió `AGENTS.md` con pautas para tareas posteriores y para actualizar las explicaciones junto con el código.
- **Por qué:** permitir comprender lo que se ha construido y mantener esa información al avanzar.
- **Archivos:** `AGENTS.md`, `README.md`, `docs/estado-actual.md`, `docs/funcionamiento-interno.md` y `docs/historial.md`.
- **Validación:** contraste de las explicaciones con `src/main.rs` y revisión de enlaces locales. Este cambio es exclusivamente documental; no requiere nuevas pruebas del intérprete.

## 2026-09-14 — Punto y coma obligatorio y println

- **Qué se hizo:** todas las instrucciones terminan en `;`. `print("texto");` escribe sin salto final y la nueva instrucción `println("texto");` añade un salto de línea.
- **Por qué:** distinguir ambas formas de imprimir y exigir un terminador explícito en cada instrucción.
- **Archivos:** `src/main.rs`, `hello.oki`, `README.md`, `AGENTS.md`, `docs/estado-actual.md`, `docs/funcionamiento-interno.md` y este historial.
- **Implementación:** tokens `Println` y `Semicolon`, comprobación del terminador en el parser, variante `Stmt::Println` y escritura diferenciada en el intérprete. El ejemplo usa `println` para conservar su salida.
- **Validación realizada:** nueve pruebas superadas, incluidos terminadores ausentes sin salida parcial, salida combinada, cadenas vacías, Unicode y errores de escritura. `cargo fmt -- --check` superado después de aplicar formato; `cargo clippy --all-targets -- -D warnings` superado; `cargo run -- hello.oki` imprime `Hello world` con salto final. Documentación contrastada con el código y enlaces locales revisados.

## 2026-09-14 — Tipos básicos y variables con tipo obligatorio

- **Qué se hizo:** tipos `int` (i64), `float` (f64), `bool`, `char` Unicode y `string`; declaraciones `tipo nombre = expresión;`, lectura de variables y reasignación con tipo fijo. Se exige un valor inicial y no hay inferencia de tipos ni conversiones implícitas. `print` y `println` aceptan los cinco tipos y variables, conservando paréntesis y punto y coma.
- **Por qué:** incorporar los tipos básicos solicitados y exigir el tipo en toda declaración, con la sintaxis elegida por el usuario. Se adaptan los valores y entornos de los capítulos 7 y 8 de *Crafting Interpreters*; una etapa propia comprueba nombres y tipos antes de ejecutar.
- **Implementación:** tokens de tipos y literales, AST para declaraciones/asignaciones/referencias, entorno de tipos y entorno de valores. Se rechazan nombres desconocidos, duplicados, incompatibilidades, enteros fuera de rango, float no finitos y caracteres vacíos o múltiples. Se admiten literales numéricos negativos y notación científica. El código se divide en módulos para conservar responsabilidades claras.
- **Archivos:** `src/main.rs`, nuevos `src/scanner.rs`, `src/parser.rs`, `src/value.rs`, `src/type_checker.rs`, `src/interpreter.rs` y `tipos.oki`; actualizados `README.md`, `docs/estado-actual.md`, `docs/funcionamiento-interno.md` y este historial.
- **Discrepancia previa corregida:** la prueba de `hello.oki` fallaba porque esperaba `Hello world`, mientras el archivo del usuario contiene `print("Hello "); println("World");`. Se conserva el archivo y se ajustan la prueba y la documentación a `Hello World`.
- **Validación realizada:** 20 pruebas superadas, con todas las combinaciones de tipos incompatibles en declaraciones y asignaciones, ausencia de salida por errores de análisis/tipos, límites numéricos, Unicode y aislamiento entre ejecuciones. `cargo fmt -- --check` superado tras aplicar `cargo fmt`; `cargo clippy --all-targets -- -D warnings` superado. `cargo run -- hello.oki` imprime `Hello World` con salto final y `cargo run -- tipos.oki` imprime los valores documentados.
- **Revisión documental:** 21 enlaces locales comprobados y nueve fragmentos de ejemplo ejecutados, confirmando tanto las salidas válidas como los rechazos documentados; `git diff --check` superado.

## 2026-09-14 — Ejemplos agrupados en examples

- **Qué se hizo:** traslado de `hello.oki` y `tipos.oki` a `examples/`, al mismo nivel que `src/`, conservando su contenido.
- **Por qué:** agrupar los programas de ejemplo en la carpeta solicitada por el usuario.
- **Archivos:** `examples/hello.oki`, `examples/tipos.oki`, rutas de las pruebas en `src/main.rs`, instrucciones y enlaces en `README.md`, `AGENTS.md`, `docs/estado-actual.md` y este historial. Las entradas anteriores conservan las rutas usadas en su momento.
- **Validación realizada:** 20 pruebas superadas; `cargo fmt -- --check` superado tras aplicar formato; `cargo clippy --all-targets -- -D warnings` superado; ambos ejemplos ejecutados desde sus nuevas rutas con la misma salida. Enlaces locales y ubicación de los archivos comprobados.


## 2026-09-14 — Operaciones básicas de los cinco tipos

- **Qué se hizo:** aritmética `+`, `-`, `*`, `/`, `%` y signos unarios para `int` y `float`; lógica `!`, `&&`, `||` para `bool`; concatenación `+` para `string`; igualdad y desigualdad para los cinco tipos, y orden para números, caracteres y cadenas. Se añaden paréntesis de agrupación y precedencia. Los operandos binarios deben tener el mismo tipo, sin conversiones implícitas.
- **Por qué:** permitir calcular y combinar los valores de los tipos existentes, conservando el tipado estricto y el recorrido separado por scanner, parser, AST, comprobador de tipos e intérprete.
- **Implementación:** tokens de operadores, nodos `Unary` y `Binary` con línea del operador, análisis descendente por niveles de precedencia, comprobación recursiva de operandos y evaluación con cortocircuito lógico. La división entera trunca hacia cero; el resto conserva el signo del dividendo. Se conserva el literal mínimo de `i64` y su resto por `-1` produce cero. Los errores de división/resto por cero, desbordamiento entero y resultados float no finitos detienen la ejecución, indicando la línea y conservando la salida anterior.
- **Archivos:** `src/scanner.rs`, `src/parser.rs`, `src/type_checker.rs`, `src/interpreter.rs`, pruebas en `src/main.rs`, nuevo `examples/operaciones.oki`, `README.md`, `docs/estado-actual.md`, `docs/funcionamiento-interno.md` y este historial.
- **Pruebas actualizadas:** se ajusta el caso que antes rechazaba operaciones ahora válidas y se añaden nueve pruebas con aritmética, precedencia, agrupación, expresiones en declaraciones/asignaciones/impresiones, comparaciones de los cinco tipos, Unicode, tablas de verdad, cortocircuito, todas las mezclas de tipos entre operadores binarios, errores numéricos y líneas de error. Se conserva la prueba de `hello.oki`.
- **Validación realizada:** 29 pruebas superadas con `cargo test`; `cargo fmt -- --check` superado tras aplicar `cargo fmt`; `cargo clippy --all-targets -- -D warnings` superado. Ejecutados `cargo run -- examples/hello.oki`, `cargo run -- examples/tipos.oki` y `cargo run -- examples/operaciones.oki`, con las salidas documentadas.
- **Revisión documental:** 23 enlaces locales comprobados y diez fragmentos de ejemplo revisados mediante ejecución, incluidos los errores intencionados; el fragmento de reasignación se comprobó con la declaración previa indicada en su contexto. `git diff --check` superado.

## 2026-09-14 — Constantes con tipo explícito

- **Qué se hizo:** declaraciones `const tipo nombre = expresión;` para los cinco tipos. Su valor se inicializa una sola vez y cualquier reasignación se rechaza antes de ejecutar el archivo, incluso si pretende guardar el mismo valor. Las declaraciones sin `const` siguen permitiendo reasignaciones.
- **Por qué:** permitir que el usuario declare valores que no puedan cambiar, conservando el tipo obligatorio, las expresiones existentes y las etapas separadas del lenguaje.
- **Implementación:** token reservado `Const`, campo `is_constant` en `Stmt::Declare` y entorno `HashMap<String, VariableInfo>` con tipo y condición de constante. El comprobador rechaza asignaciones a constantes indicando la línea del nombre. El intérprete sigue recibiendo un AST validado y no necesita duplicar esta comprobación. Se conservan las reglas de inicialización, tipos compatibles y nombres únicos.
- **Archivos:** `src/scanner.rs`, `src/parser.rs`, `src/type_checker.rs`, pruebas en `src/main.rs`, nuevo `examples/constantes.oki`, `README.md`, `docs/estado-actual.md`, `docs/funcionamiento-interno.md` y este historial.
- **Validación realizada:** 33 pruebas superadas con `cargo test`, incluidas cuatro nuevas pruebas de constantes y la ampliación del rechazo de inicializadores de tipos incompatibles. Se comprueban los cinco tipos, copias independientes, expresiones, declaraciones incompletas, nombres duplicados, palabra reservada y reasignaciones sin salida parcial. Se conserva la prueba de `hello.oki`. `cargo fmt -- --check` superado tras aplicar `cargo fmt`; `cargo clippy --all-targets -- -D warnings` superado. Ejecutados `cargo run -- examples/hello.oki` y `cargo run -- examples/constantes.oki` con las salidas documentadas.
- **Revisión documental:** 25 enlaces locales comprobados y fragmentos de constantes de la documentación verificados mediante ejecución, incluido el rechazo intencionado sin salida parcial. `git diff --check` superado.

## 2026-09-14 — Arrays con tipo de elemento explícito

- **Qué se hizo:** tipos como `int[]`, `string[]` e `int[][]`, literales `[1, 2]`, arrays vacíos con contexto de tipo, lectura y escritura mediante índices desde cero, copia independiente y reasignación completa. Los elementos deben tener el mismo tipo; la longitud puede variar al sustituir el array y no forma parte del tipo. Se admite impresión e igualdad por contenido, orden y longitud. `const` impide modificar el array y cualquiera de sus elementos anidados.
- **Por qué:** añadir el tipo array solicitado, conservando los tipos obligatorios, la ausencia de conversiones implícitas y las etapas separadas del intérprete.
- **Implementación:** tokens de corchetes y coma; `Type::Array(Box<Type>)`, `Value::Array(Vec<Value>)`, expresiones `Array` e `Index` y lista de índices en `Stmt::Assign`. El parser reconoce accesos con precedencia mayor que los unarios. El comprobador propaga el tipo esperado a los elementos de los literales y exige índices `int`. El intérprete comprueba límites al leer y escribir y clona los valores al copiar, también en profundidad. Los errores de límites indican la línea del corchete y conservan la salida previa. Los destinos se validan antes de evaluar el valor asignado, sin modificaciones parciales si falla la instrucción.
- **Límites documentados:** un vacío sin contexto de tipo se rechaza; el contexto no se propaga entre operandos de operadores. No hay consulta de longitud, métodos para añadir/eliminar elementos, intervalos, coma final ni longitud declarada. El formato de impresión conserva el texto sin escapes y no garantiza ser código reutilizable.
- **Archivos:** `src/scanner.rs`, `src/parser.rs`, `src/value.rs`, `src/type_checker.rs`, `src/interpreter.rs`, pruebas en `src/main.rs`, nuevo `examples/arrays.oki`, `README.md`, `docs/estado-actual.md`, `docs/funcionamiento-interno.md` y este historial.
- **Validación realizada:** 42 pruebas superadas con `cargo test`, incluidas nueve nuevas de arrays. Se comprueban los cinco tipos de elementos, vacíos y anidados, acceso y modificación, copias, constantes, igualdad, incompatibilidades, sintaxis, índices negativos y extremos, líneas de error, orden de evaluación y cortocircuito. Se conserva la prueba de `hello.oki`. `cargo fmt -- --check` superado tras aplicar formato; `cargo clippy --all-targets -- -D warnings` superado. Ejecutados `cargo run -- examples/hello.oki` y `cargo run -- examples/arrays.oki` con las salidas documentadas.
- **Revisión documental:** 27 enlaces locales comprobados, incluidos sus anclajes, y 17 fragmentos ejecutados, con los rechazos intencionados sin salida parcial y la declaración previa añadida al fragmento que depende de ella. `git diff --check` superado.

## 2026-09-14 — Condiciones if/else y ámbitos de bloque

- **Qué se hizo:** instrucciones `if (condición) { ... }`, con `else if`/`else` opcionales. La condición debe ser `bool`; las ramas van siempre entre llaves y el `if` no lleva `;` final. Cada bloque abre un ámbito propio: los nombres declarados dentro solo existen ahí y pueden ocultar a los de fuera; las variables exteriores se pueden reasignar dentro, salvo las constantes. La comprobación de tipos recorre las dos ramas antes de ejecutar.
- **Por qué:** añadir el control de flujo solicitado por el usuario, conservando las etapas separadas y el tipado estricto. Es el primer uso de ámbitos anidados, que hasta ahora se resolvían con una única tabla global.
- **Implementación:** tokens `If`, `Else`, `LeftBrace` y `RightBrace`; `Stmt::If` con condición, rama `then` y `else` opcional; `if_statement()` y `block()` en el parser, que no exige `;` tras el `if`. El comprobador pasa a `Vec<HashMap<String, VariableInfo>>` y el intérprete a `Vec<HashMap<String, Value>>`, con `push`/`pop` por bloque. Se permite ocultar nombres comprobando duplicados solo en el ámbito actual. La condición se comprueba contra `bool` y señala la línea del `if`.
- **Límites:** no hay bloques sueltos, `while`/`for` ni declaraciones sin inicializador. La idea de un modificador `dependant` con asignación obligatoria en todas las ramas queda pospuesta como avance aparte.
- **Archivos:** `src/scanner.rs`, `src/parser.rs`, `src/type_checker.rs`, `src/interpreter.rs`, pruebas en `src/main.rs`, nuevo `examples/condiciones.oki`, `README.md`, `docs/estado-actual.md`, `docs/funcionamiento-interno.md` y este historial.
- **Validación realizada:** 46 pruebas superadas con `cargo test`, incluidas cuatro nuevas de condiciones. `cargo fmt -- --check` superado tras aplicar `cargo fmt`; `cargo clippy --all-targets -- -D warnings` superado. Ejecutados `cargo run -- examples/hello.oki` (salida `Hello World`) y `cargo run -- examples/condiciones.oki` con la salida documentada.
- **Revisión documental:** 29 enlaces locales comprobados, incluidos sus anclajes. Verificados mediante ejecución el fragmento de `if`/`else` de `estado-actual.md` (imprime `mayor de edad`, `adultez`, `adulto`), el recorrido de `funcionamiento-interno.md` (imprime `10` y `1`), los rechazos intencionados sin salida parcial y el ejemplo `condiciones.oki`. `git diff --check` superado.

## 2026-09-14 — Bucles while, for y foreach

- **Qué se hizo:** tres instrucciones de repetición. `while (condición) { ... }` repite mientras la condición sea `bool`. `for` de estilo C, `for (inicialización; condición; actualización) { ... }`, con las tres partes obligatorias; la inicialización admite una declaración o una asignación, la condición debe ser `bool` y la actualización es una asignación (no hay `++` ni `+=`). `foreach (tipo nombre in array) { ... }` recorre un array indicando el tipo de cada elemento. En los tres el cuerpo es un bloque y no llevan `;` final.
- **Por qué:** completar el control de flujo solicitado por el usuario. Hasta ahora solo existía `if`, de modo que no había forma de repetir instrucciones ni de recorrer arrays.
- **Implementación:** tokens `While`, `For`, `Foreach` e `In`; nodos `Stmt::While`, `Stmt::For` y `Stmt::Foreach`; en el parser, `while_statement()`, `for_statement()` y `foreach_statement()`, con `assignment()` separada de `statement()` para reutilizarla en la cabecera del `for` y `array_type()` compartida con las declaraciones. El comprobador abre ámbitos propios: uno para el contador del `for` que envuelve condición, actualización y cuerpo, y otro para la variable del `foreach`; `require_bool()` unifica el mensaje de condición incorrecta. El intérprete añade `execute_for()`, `execute_foreach()` y `evaluate_bool()`, y evalúa el array del `foreach` una sola vez para recorrer una copia.
- **Límites:** no hay `break` ni `continue`, ni `for (;;)` con partes vacías; los tres bucles exigen su bloque. El contador del `for` y la variable del `foreach` no existen fuera del bucle. `foreach` solo admite arrays y el tipo declarado debe coincidir con el del elemento.
- **Archivos:** `src/scanner.rs`, `src/parser.rs`, `src/type_checker.rs`, `src/interpreter.rs`, pruebas en `src/main.rs`, nuevo `examples/bucles.oki`, `README.md`, `docs/estado-actual.md`, `docs/funcionamiento-interno.md` y este historial.
- **Validación realizada:** 52 pruebas superadas con `cargo test`, incluidas cinco nuevas de bucles. Se comprueban el ejemplo, la repetición de `while`, el orden del `for` y la actualización por índice, el ámbito propio y la ocultación de nombres, el recorrido y la copia de elementos de `foreach` (con arrays anidados y vacíos), la comprobación del tipo de elemento, el rechazo de contadores constantes y las condiciones y sintaxis inválidas. `cargo fmt -- --check` superado tras aplicar `cargo fmt`; `cargo clippy --all-targets -- -D warnings` superado. Ejecutados `cargo run -- examples/hello.oki` (salida `Hello World`) y `cargo run -- examples/bucles.oki` con la salida documentada.
- **Revisión documental:** enlaces locales comprobados y fragmentos de `while`, `for` y `foreach` de la documentación verificados mediante ejecución, incluido el rechazo de `while (1)` sin salida parcial y el ejemplo `bucles.oki`. `git diff --check` superado.

## 2026-09-14 — Asignaciones abreviadas e incremento/decremento

- **Qué se hizo:** operadores `+=`, `-=`, `++` y `--` como instrucciones sobre variables ya declaradas, también en elementos de array (`numeros[0]++`, `tabla[0][1] += 2`). `+=` sigue las reglas de `+` (`int`, `float` y concatenación de `string`); `-=` las de `-` (`int` y `float`); `++` y `--` suman o restan una unidad y solo admiten `int` o `float`. La actualización de un `for` admite las cuatro formas. Se rechazan constantes y tipos incompatibles antes de ejecutar.
- **Por qué:** completar la sintaxis de asignación solicitada por el usuario sin introducir expresiones de asignación: se mantiene la decisión de que la asignación es una instrucción.
- **Implementación:** tokens `PlusPlus`, `PlusEqual`, `MinusMinus` y `MinusEqual` con `compound_or_single()`; nodos `Stmt::CompoundAssign` e `Stmt::Increment` con `AssignOp` e `IncrementOp`, que conocen su operación binaria equivalente; `assignment()` reconoce las cuatro formas y se reutiliza en el `for`. El comprobador extrae `binary_result()` (reglas de tipos compartidas con los operadores binarios) y `assignment_target()` (búsqueda, rechazo de constantes e índices). El intérprete extrae `resolve_target()`, `target_value()` y `write_target()`, reutilizados por asignación, compuesta e incremento; el paso de una unidad conserva el tipo (`1` en `int`, `1.0` en `float`).
- **Cambio de comportamiento:** `--` pasa a ser el operador de decremento, de modo que `--1` y `1--2`, que antes eran doble negación y una resta, ahora se rechazan. Se retiran esos dos casos de la prueba de precedencia y se ajusta la documentación.
- **Límites:** no hay prefijo `++x`/`--x`, ni `*=`, `/=`, `%=`, ni expresiones de asignación (`int y = x++;`). Son operaciones sobre el destino, no expresiones con valor.
- **Archivos:** `src/scanner.rs`, `src/parser.rs`, `src/type_checker.rs`, `src/interpreter.rs`, pruebas en `src/main.rs`, nuevo `examples/asignaciones.oki`, `README.md`, `docs/estado-actual.md`, `docs/funcionamiento-interno.md` y este historial.
- **Validación realizada:** 61 pruebas superadas con `cargo test`, incluidas seis nuevas de asignaciones abreviadas. `cargo fmt -- --check` superado tras aplicar `cargo fmt`; `cargo clippy --all-targets -- -D warnings` superado. Ejecutados `cargo run -- examples/hello.oki` (salida `Hello World`) y `cargo run -- examples/asignaciones.oki` con la salida documentada.
- **Revisión documental:** enlaces locales y anclajes comprobados y fragmentos de `+=`, `-=`, `++` y `--` de la documentación verificados mediante ejecución, incluidos los rechazos intencionados sin salida parcial.

## 2026-09-15 — Biblioteca estándar std::Array y método len

- **Qué se hizo:** `import std::Array;` habilita `numeros.len()` y `std::Array::len(numeros)`. Un `use std::Array;` posterior añade `Array::len(numeros)`. `len` devuelve `int`, admite todos los arrays actuales, vacíos declarados, constantes y anidados, y cuenta solo el nivel consultado. Las operaciones básicas de arrays siguen funcionando sin importar nada.
- **Por qué:** introducir la organización de biblioteca estándar y la llamada de métodos sobre valores solicitadas por el usuario. `import` habilita la biblioteca y `use` permite omitir `std::` en la ruta corta, conservando el tipado estricto y las etapas separadas.
- **Implementación:** tokens `Import`, `Use`, `ColonColon` y `Dot`; `Stmt::Import` y `Expr::LibraryCall` con ruta, receptor opcional y argumentos. El parser reconoce rutas y encadena métodos con índices. El nuevo `src/stdlib.rs` concentra las marcas de disponibilidad, la resolución de nombres, la comprobación de argumentos y tipo y la consulta de longitud. El comprobador exige directivas globales y previas al uso; el intérprete evalúa el array una vez y convierte su longitud a `i64` con comprobación de rango.
- **Reglas y límites:** `use` requiere un `import` anterior; repetir directivas es válido y cada ejecución empieza sin bibliotecas habilitadas. Las rutas distinguen mayúsculas y se resuelven aparte de las variables. Solo se incluye `std::Array` con `len`: no hay carga de archivos externos, métodos de modificación, funciones del usuario, alias personalizados ni comodines. Las llamadas se usan como expresiones. Un literal `[]` sin contexto sigue siendo inválido. La evaluación conserva las copias de arrays existentes, también al consultar longitud; no se han optimizado ni medido prestaciones. `import` y `use` pasan a ser palabras reservadas.
- **Archivos:** `src/scanner.rs`, `src/parser.rs`, `src/type_checker.rs`, `src/interpreter.rs`, nuevo `src/stdlib.rs`, pruebas en `src/main.rs`, nuevo `examples/biblioteca_arrays.oki`, `README.md`, `docs/estado-actual.md`, `docs/funcionamiento-interno.md` y este historial.
- **Validación realizada:** 70 pruebas superadas con `cargo test`, incluidas nueve nuevas de biblioteca. Cubren las tres formas de llamada, orden de importaciones y `use`, aislamiento, tipos de elementos, vacíos, constantes, anidación, composición con expresiones y bucles, errores de nombres, argumentos, tipos y sintaxis, líneas y ausencia de salida parcial ante errores estáticos, cortocircuito y conservación de salida ante errores de ejecución. Se conserva la prueba de `hello.oki`. `cargo fmt -- --check` superado tras aplicar `cargo fmt`; `cargo clippy --all-targets -- -D warnings` superado. Ejecutados `cargo run -- examples/hello.oki` (salida `Hello World`) y `cargo run -- examples/biblioteca_arrays.oki` (3, 3, 3, 10, 20, 30 y 0 en líneas separadas).
- **Revisión documental:** 38 enlaces locales con sus anclajes comprobados y cuatro fragmentos nuevos ejecutados, incluido el rechazo intencionado por falta de importación. `git diff --check` superado.

## 2026-09-15 — Añadir y eliminar elementos con push y pop

- **Qué se hizo:** `push` añade al final de un array modificable y `pop` elimina y devuelve su último elemento. Se admiten las mismas formas que `len`: `numeros.push(4)` / `numeros.pop()`, `std::Array::push(numeros, 4)` / `std::Array::pop(numeros)` y las rutas cortas `Array::push` / `Array::pop` con `use`. Se mantienen las reglas de importación previa. Las llamadas pueden ser instrucciones con `;`; `push` no devuelve valor y se rechaza dentro de expresiones que necesitan uno.
- **Por qué:** permitir construir listas durante la ejecución, con ambas sintaxis solicitadas, manteniendo el tipo de elemento explícito y la protección de constantes.
- **Implementación:** `Stmt::Call` y descomposición del destino con `Expr::into_target()`; variantes `Push` y `Pop` en `ArrayFunction`; comprobación del tipo del elemento, contexto para insertar subarrays vacíos y rechazo de constantes o temporales. `evaluate_call()` obtiene el almacenamiento original y la biblioteca modifica su `Vec`. `Option<Type>` y `Option<Value>` representan la ausencia de resultado de `push`, sin añadir `void` al lenguaje. El scanner y la representación de los valores no necesitan cambios.
- **Orden y errores:** los índices del destino se evalúan una sola vez antes del argumento de `push`. Como `pop` puede modificar arrays dentro de expresiones, se comprueban de nuevo las posiciones antes de leer o escribir un destino guardado. Un destino invalidado informa de un error en la línea de la variable raíz; `pop` de un vacío falla en la línea del método. Se conserva la salida previa y no se deshacen efectos ya completados. Se mantienen las copias independientes, el cortocircuito y la copia de entrada de `foreach`.
- **Archivos:** `src/parser.rs`, `src/stdlib.rs`, `src/type_checker.rs`, `src/interpreter.rs`, pruebas en `src/main.rs`, nuevo `examples/modificar_arrays.oki`, `README.md`, `docs/estado-actual.md`, `docs/funcionamiento-interno.md` y este historial.
- **Validación realizada:** 77 pruebas superadas con `cargo test`, incluidas siete nuevas de modificación y el ajuste del caso que antes consideraba `push` desconocido. Cubren ambas sintaxis, las rutas completa y corta, todos los tipos básicos, arrays vacíos y anidados, copias, constantes, ámbitos, bucles, orden de evaluación, cortocircuito, errores de tipos y ejecución y destinos invalidados por llamadas interiores. Se conserva la prueba de `hello.oki`. `cargo fmt -- --check` superado tras aplicar formato y `cargo clippy --all-targets -- -D warnings` superado. Ejecutados `cargo run -- examples/hello.oki` y `cargo run -- examples/modificar_arrays.oki`, con las salidas documentadas.
- **Revisión documental:** comprobada la existencia de los archivos de 41 enlaces locales; ejecutados los tres fragmentos nuevos de README, estado actual y funcionamiento interno, comparando sus salidas con las documentadas. `git diff --check` superado.

## 2026-09-15 — Conversiones explícitas con std::Casting

- **Qué se hizo:** biblioteca `std::Casting` con funciones `int(valor)`, `float(valor)`, `bool(valor)`, `char(valor)` y `string(valor)`, además de `valor.cast(tipo)`. `import std::Casting;` habilita ambas formas y las rutas completas; `use std::Casting;` añade las rutas cortas, como `Casting::float(edad)`. Las conversiones devuelven valores nuevos y conservan el tipado estricto de declaraciones, asignaciones y operadores.
- **Por qué:** permitir mezclar datos intencionadamente mediante conversiones explícitas, por ejemplo `float(edad) + 0.5` o `"Edad: " + string(edad)`, con la sintaxis de método solicitada.
- **Implementación:** `Expr::Cast` conserva la ruta, el tipo de destino y la expresión de entrada. El parser reconoce las llamadas con un tipo reservado y los métodos `cast` con un tipo como argumento. El scanner distingue el punto decimal del punto de `25.cast(float)` mirando el siguiente carácter. `StandardLibrary` sustituye a `ArrayLibrary` y mantiene accesos independientes para Array y Casting. El nuevo `src/stdlib/casting.rs` separa la compatibilidad de tipos de la conversión efectiva; el intérprete evalúa la entrada una sola vez.
- **Reglas y límites:** identidad entre tipos básicos, conversiones numéricas entre `int` y `float`, tipos básicos a texto, texto a cada tipo básico y conversiones entre `char` e `int` mediante valores escalares Unicode. `float` a `int` trunca hacia cero y comprueba el intervalo antes de convertir; `int` a `float` puede perder precisión. No se convierten arrays completos ni booleanos a números. Los pares inválidos fallan antes de ejecutar; el contenido inválido y los rangos se comprueban en ejecución, conservando línea y salida previa. La tabla completa está en `docs/estado-actual.md`.
- **Archivos:** `src/scanner.rs`, `src/parser.rs`, `src/stdlib.rs`, nuevo `src/stdlib/casting.rs`, `src/type_checker.rs`, `src/interpreter.rs`, pruebas en `src/main.rs`, nuevo `examples/conversiones.oki`, `README.md`, `docs/estado-actual.md`, `docs/funcionamiento-interno.md` y este historial.
- **Validación realizada:** 85 pruebas superadas con `cargo test`, incluidas ocho nuevas de Casting y la prueba conservada de `hello.oki`. Cubren las cuatro formas de llamada, pares admitidos, importaciones independientes y ordenadas, constantes, composición con Array y sus efectos, evaluación única, cortocircuito, sintaxis, tipado estricto, límites de enteros y float, Unicode, líneas de error y salida previa. `cargo fmt -- --check` superado tras aplicar formato y `cargo clippy --all-targets -- -D warnings` superado. Ejecutados `cargo run -- examples/hello.oki` y `cargo run -- examples/conversiones.oki`, con las salidas documentadas.
- **Revisión documental:** 46 enlaces locales con sus anclajes comprobados. Ejecutados y verificados los cuatro fragmentos nuevos de Casting en README, estado actual y funcionamiento interno, incluido el error intencionado al convertir `"hola"` a `int` y su salida previa.
