import std::Array;

int[] numeros = [10, 20, 30];
println(numeros.len());
println(std::Array::len(numeros));

use std::Array;
println(Array::len(numeros));

for (int i = 0; i < numeros.len(); i++) {
    println(numeros[i]);
}

const int[] vacio = [];
println(vacio.len());
